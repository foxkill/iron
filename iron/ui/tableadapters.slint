export global DetailsTableAdapter {
    in property <[[StandardListViewItem]]> row_data;
}

export global QualityTableAdapter {
    callback copy_cusip_to_clipboard(int);
    in-out property <[[StandardListViewItem]]> row_data;
}

export global SecuritiesTableAdapter {
    in property <[[StandardListViewItem]]> row_data;
}