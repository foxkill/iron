import { LineEdit, Button, HorizontalBox } from "std-widgets.slint";

export component CusipInput inherits HorizontalBox {
    forward-focus: cusip;
    Text { vertical-alignment: center; text: "Enter CUSIP:"; }
    cusip := LineEdit { placeholder-text: "Enter CUSIP here..."; }
    Button { text: "Search"; enabled: true; }
}
